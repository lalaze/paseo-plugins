{
  "name": "paseo-file-upload",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Workspace file transfer panel for Paseo: browse, upload, and download files on the daemon host",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/*.test.ts",
    "preview": "node --import tsx dev/preview.ts",
    "check": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "@getpaseo/plugin": "0.8.0",
    "@types/node": "^22.0.0",
    "@types/react": "~19.2.0",
    "@types/react-dom": "^19.1.0",
    "esbuild": "^0.25.12",
    "react": "19.1.0",
    "react-dom": "^19.1.0",
    "react-native": "0.81.5",
    "tsx": "^4.20.0",
    "typescript": "^5.9.3",
    "zod": "^4.4.3"
  }
}
