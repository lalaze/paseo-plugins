# 验证记录

2026-09-14，在独立的 Paseo 0.8.0 daemon 中加载本工作区的实际插件代码，使用真实 Codex MCP 接入和原生网页客户端验证。以下图片是运行截图，不是生成图或模拟页面。

- [主对话](native-chat.png)：正常聊天、工具调用及用户最终验收。
- [子 Agent 入口](native-children.png)：真实任务状态与打开执行会话入口。
- [协作设置](native-settings.png)：原生设置入口和已有设置编辑器。
- [窄屏子 Agent](native-children-mobile.png)：390px 视口下的原生聊天与展开列表。

真实闭环：从原生输入框要求在临时仓库创建 `hello.txt` → 主 Agent 调用 MCP 启动任务和提交方案 → 用户回复“批准方案” → 一个真实子 Agent 执行 → 主 Agent 检查实际文件并提交审核 → 用户回复“验收通过” → 后台状态成为 `completed`。文件内容验证为十六进制 `68 65 6c 6c 6f 0a`，只新增该文件，没有提交代码。

另验证了插件重载后待验收状态恢复、主会话与子会话跳转、协作设置打开、390px 设置及聊天无横向溢出。没有插件渲染错误；浏览器中只有宿主原有的推送通知和原生动画模块提示。

自动检查：145 项测试通过，类型检查及 Paseo 官方编译器客户端／服务端／模块边界校验通过。最后的前端缓存主机隔离修改另完成类型检查、构建和真实宿主 UI 回归。

测试数据、会话及代码目录均位于 `/tmp/director-native-host` 和 `/tmp/director-native-repo`。常用宿主的 Git 安装版插件未被替换。测试完成后关闭隔离 daemon。
