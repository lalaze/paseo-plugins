# 验证记录

2026-09-14，在独立的 Paseo 0.8.0 daemon 中加载本工作区的实际插件代码，使用真实 Codex MCP 接入和原生网页客户端验证。以下图片是运行截图，不是生成图或模拟页面。

- [主对话](native-chat.png)：正常聊天、工具调用及用户最终验收。
- [子 Agent 入口](native-children.png)：真实任务状态与打开执行会话入口。
- [协作设置](native-settings.png)：原生设置入口和已有设置编辑器。
- [窄屏子 Agent](native-children-mobile.png)：390px 视口下的原生聊天与展开列表。

真实闭环：从原生输入框要求在临时仓库创建 `hello.txt` → 主 Agent 调用 MCP 启动任务和提交方案 → 用户回复“批准方案” → 一个真实子 Agent 执行 → 主 Agent 检查实际文件并提交审核 → 用户回复“验收通过” → 后台状态成为 `completed`。文件内容验证为十六进制 `68 65 6c 6c 6f 0a`，只新增该文件，没有提交代码。

另验证了插件重载后待验收状态恢复、主会话与子会话跳转、协作设置打开、390px 设置及聊天无横向溢出。没有插件渲染错误；浏览器中只有宿主原有的推送通知和原生动画模块提示。

自动检查：145 项测试通过，类型检查及 Paseo 官方编译器客户端／服务端／模块边界校验通过。最后的前端缓存主机隔离修改另完成类型检查、构建和真实宿主 UI 回归。

测试数据、会话及代码目录均位于 `/tmp/director-native-host` 和 `/tmp/director-native-repo`。常用宿主的 Git 安装版插件未被替换。测试完成后关闭隔离 daemon。

## 原地接管验证（2026-09-15）

在独立的 `/tmp/director-takeover-host`、`/tmp/director-takeover-repo` 中，通过实际 Paseo 网页输入 `/director` 验证已有普通会话的接管。主模型为 Codex GPT-6-Astra，权限模式 Auto-review。

- 接管前后会话 ID、标题、模型及权限一致，只有一个协作主会话。接管前记住的测试口令，接管后仍能正确回答。
- 主 Agent 在默认命令沙箱内通过文件桥接调用协作工具，不需要网络权限。接管只查询状态，不启动任务；重载并重新同步仍使用原会话。
- 在同一对话提交临时文件任务，方案批准前保持暂停；批准后由唯一执行 Agent 创建 `takeover.txt`，主对话审核并接受用户验收，状态最终为 `completed`。文件内容为 `63 65 64 61 72 2d 34 32 0a`，只新增该文件，无任务代码提交。
- 本次验证发现并修复了执行上下文未提供方案批准记录的问题；修复后沿用同一子任务和执行 Agent 完成，没有重建任务。执行提示和状态工具现在提供 `workflowEvidence.planApproval`。

最终结构化结果见 [takeover-proof.json](takeover-proof.json)，页面见 [native-takeover.png](native-takeover.png)。157 项自动测试、类型检查和 Paseo 官方编译全部通过。

验证只加载隔离实例中的目录安装版，常用宿主的 Git 安装版未被替换。结束后关闭隔离 daemon。

### 保存后切换设置标签回归

在隔离 Paseo 实例中使用真实浏览器：编辑设计提示词 → 保存 → 切换执行标签 → 编辑执行提示词 → 再次保存 → 切换审核标签 → 返回前两个标签核对内容。两处内容均保留，草稿状态显示“设置与已保存内容一致”，无“另一窗口”误报。截图：`settings-save-tabs.png`。完整检查通过 163 项测试、类型检查和官方构建；真正的跨窗口草稿冲突仍由回归测试验证。
